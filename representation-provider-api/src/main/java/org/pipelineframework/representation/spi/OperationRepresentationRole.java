package org.pipelineframework.representation.spi;

/** Direction of one canonical/connector-wire representation boundary. */
public enum OperationRepresentationRole {
    REQUEST, RESPONSE, CALLBACK
}
