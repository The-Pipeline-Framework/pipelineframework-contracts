# The Pipeline Framework Contracts

This repository will publish stable semantic and runtime contract artifacts shared by the TPF compiler, customer-facing runtimes, and future infrastructure workers. It is being bootstrapped in green, staged slices; no source has moved yet.
